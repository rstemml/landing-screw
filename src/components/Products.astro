---
const products = [
  {
    title: "Industrieschrauben",
    description: "Hochfeste Schrauben für anspruchsvolle industrielle Anwendungen. DIN- und ISO-genormt.",
    icon: "🔩"
  },
  {
    title: "Spezialschrauben",
    description: "Maßgefertigte Lösungen für spezielle Anforderungen in Maschinenbau und Konstruktion.",
    icon: "⚙️"
  },
  {
    title: "Edelstahlschrauben",
    description: "Korrosionsbeständige Schrauben für Außen- und Feuchträume in A2 und A4 Qualität.",
    icon: "✨"
  },
  {
    title: "Befestigungstechnik",
    description: "Dübel, Anker und Befestigungssysteme für Beton, Mauerwerk und Fassaden.",
    icon: "🔧"
  },
  {
    title: "Normteile",
    description: "Umfangreiches Sortiment an Muttern, Unterlegscheiben und Gewindestangen.",
    icon: "⚡"
  },
  {
    title: "Verbindungselemente",
    description: "Nieten, Blindnieten und Schweißmuttern für dauerhafte Verbindungen.",
    icon: "🔨"
  }
];
---

<section id="produkte" class="section products">
  <div class="container">
    <div class="section-header">
      <h2>Unsere Produkte</h2>
      <p class="section-subtitle">Qualität und Vielfalt für jeden Bedarf</p>
    </div>
    <div class="products-grid">
      {products.map(product => (
        <div class="product-card">
          <div class="product-icon">{product.icon}</div>
          <h3>{product.title}</h3>
          <p>{product.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .products {
    background-color: white;
  }

  .section-header {
    text-align: center;
    margin-bottom: var(--spacing-lg);
  }

  .section-header h2 {
    color: var(--color-primary);
    text-transform: uppercase;
    letter-spacing: 1px;
    position: relative;
    display: inline-block;
  }

  .section-header h2::after {
    content: '';
    position: absolute;
    bottom: -10px;
    left: 50%;
    transform: translateX(-50%);
    width: 60px;
    height: 4px;
    background-color: var(--color-accent);
  }

  .section-subtitle {
    color: var(--color-gray);
    font-size: 1.2rem;
    margin-top: var(--spacing-md);
  }

  .products-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--spacing-md);
    margin-top: var(--spacing-lg);
  }

  .product-card {
    background: linear-gradient(135deg, #f8f9fa 0%, #ffffff 100%);
    padding: var(--spacing-md);
    border-radius: 8px;
    border: 2px solid var(--color-gray-light);
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .product-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(90deg, var(--color-accent) 0%, var(--color-accent-dark) 100%);
    transform: scaleX(0);
    transition: transform 0.3s ease;
  }

  .product-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.15);
    border-color: var(--color-accent);
  }

  .product-card:hover::before {
    transform: scaleX(1);
  }

  .product-icon {
    font-size: 3rem;
    margin-bottom: var(--spacing-sm);
  }

  .product-card h3 {
    color: var(--color-primary);
    margin-bottom: var(--spacing-sm);
    font-size: 1.3rem;
  }

  .product-card p {
    color: var(--color-gray);
    line-height: 1.6;
  }

  @media (max-width: 768px) {
    .products-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
