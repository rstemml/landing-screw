---
---

<section id="kontakt" class="section contact">
  <div class="container">
    <div class="section-header">
      <h2>Kontakt</h2>
      <p class="section-subtitle">Wir sind für Sie da</p>
    </div>
    <div class="contact-content">
      <div class="contact-info">
        <div class="info-card">
          <div class="info-icon">📍</div>
          <h3>Adresse</h3>
          <p>
            Schraubenkönig Dresden GmbH<br />
            Industriestraße 42<br />
            01129 Dresden
          </p>
        </div>
        <div class="info-card">
          <div class="info-icon">📞</div>
          <h3>Telefon</h3>
          <p>
            Tel: +49 (0) 351 / 123 456 - 0<br />
            Fax: +49 (0) 351 / 123 456 - 99
          </p>
        </div>
        <div class="info-card">
          <div class="info-icon">✉️</div>
          <h3>E-Mail</h3>
          <p>
            info@schraubenkoenig-dresden.de<br />
            verkauf@schraubenkoenig-dresden.de
          </p>
        </div>
        <div class="info-card">
          <div class="info-icon">🕐</div>
          <h3>Öffnungszeiten</h3>
          <p>
            Mo - Fr: 07:00 - 18:00 Uhr<br />
            Sa: 08:00 - 13:00 Uhr
          </p>
        </div>
      </div>
      <div class="contact-form">
        <h3>Anfrage senden</h3>
        <form>
          <div class="form-group">
            <input type="text" placeholder="Ihr Name *" required />
          </div>
          <div class="form-group">
            <input type="email" placeholder="Ihre E-Mail *" required />
          </div>
          <div class="form-group">
            <input type="tel" placeholder="Telefon" />
          </div>
          <div class="form-group">
            <textarea rows="5" placeholder="Ihre Nachricht *" required></textarea>
          </div>
          <button type="submit" class="btn btn-primary">Nachricht senden</button>
        </form>
      </div>
    </div>
  </div>
</section>

<style>
  .contact {
    background-color: white;
  }

  .contact-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--spacing-lg);
    margin-top: var(--spacing-lg);
  }

  .contact-info {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--spacing-md);
  }

  .info-card {
    background: linear-gradient(135deg, #f8f9fa 0%, #ffffff 100%);
    padding: var(--spacing-md);
    border-radius: 8px;
    border-left: 4px solid var(--color-accent);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
    transition: all 0.3s ease;
  }

  .info-card:hover {
    transform: translateX(8px);
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.12);
  }

  .info-icon {
    font-size: 2.5rem;
    margin-bottom: var(--spacing-sm);
  }

  .info-card h3 {
    color: var(--color-primary);
    font-size: 1.2rem;
    margin-bottom: var(--spacing-xs);
  }

  .info-card p {
    color: var(--color-gray);
    line-height: 1.6;
  }

  .contact-form {
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-secondary) 100%);
    padding: var(--spacing-lg);
    border-radius: 8px;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
  }

  .contact-form h3 {
    color: white;
    margin-bottom: var(--spacing-md);
    font-size: 1.5rem;
  }

  .form-group {
    margin-bottom: var(--spacing-sm);
  }

  .form-group input,
  .form-group textarea {
    width: 100%;
    padding: 1rem;
    border: 2px solid transparent;
    border-radius: 4px;
    font-size: 1rem;
    font-family: inherit;
    transition: all 0.3s ease;
    background-color: white;
  }

  .form-group input:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: var(--color-accent);
    box-shadow: 0 0 0 3px rgba(230, 57, 70, 0.1);
  }

  .form-group textarea {
    resize: vertical;
    min-height: 120px;
  }

  .contact-form .btn-primary {
    width: 100%;
    margin-top: var(--spacing-sm);
    background-color: var(--color-accent);
    color: white;
    cursor: pointer;
  }

  .contact-form .btn-primary:hover {
    background-color: var(--color-accent-dark);
  }

  @media (max-width: 968px) {
    .contact-content {
      grid-template-columns: 1fr;
    }
  }
</style>
